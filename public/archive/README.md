# Sprite archive

Superseded / replaced sprite sheets live here so `public/` only holds the live
assets the game actually loads. Nothing in this folder is referenced by the build.

(Most regenerations overwrite the original filename in place, so there are usually
no orphans — this folder is for cases where a sheet is replaced by a differently
named one.)
